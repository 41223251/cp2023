#include <stdio.h>

int main() 
{
    int x = 25; // 直接賦值為 25
    int y = 88; // 直接賦值為 88
    int result;

    result = x * y; // 計算 x 和 y 的乘積

    // 輸出結果
    printf("Product of %d and %d = %d\n", x, y, result);

    return 0; // 表示正確結束
}
